#include <bits/stdc++.h>
using namespace std;

int main (){
    string s = "Hello";
    string t = "World";
    // s += t; // s = s + t
    // s.append(t);
    // s.push_back('a');
    // s.insert(5, ", ");
    // s.insert(s.size(), "!");
    // s.pop_back();
    // s.replace(7, 5, "C++");
    // s = t;
    // s.assign(t);
    // s.erase(0, 3); // starting index, number of characters
    // s.replace(s.begin(), s.begin() + 2, "lo");
    s.is_empty();
    cout << s << endl;
    return 0;
}